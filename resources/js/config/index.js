module.exports = {
    /**
     * Export the particle configurations
     *
     * Format: <element id>: require("particle_config.json")
     */
    particles: {
        "header_particles": require("./particles.json")
    }
};
